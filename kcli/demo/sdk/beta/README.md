# Beta Demo SDK

Exists for CI and as a minimal reference for integrating an SDK add-on with KcliSDK.

This SDK demonstrates:

- a reusable `--beta` inline parser
- required-value handlers such as `--beta-profile`
- handler-level validation for `--beta-workers`
