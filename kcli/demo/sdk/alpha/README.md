# Alpha Demo SDK

Exists for CI and as a minimal reference for integrating an SDK add-on with KcliSDK.

This SDK demonstrates the library-side pattern:

- expose `GetInlineParser()`
- define the `--alpha` inline root
- register required and optional inline handlers
