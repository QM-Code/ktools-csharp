# Gamma Demo SDK

Exists for CI and as a minimal reference for integrating an SDK add-on with KcliSDK.

This SDK demonstrates:

- a reusable `--gamma` inline parser
- optional-value handling for `--gamma-strict`
- required-value handling for `--gamma-tag`
