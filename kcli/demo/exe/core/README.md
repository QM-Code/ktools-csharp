# Core Demo

Basic integration showcase for KcliSDK plus the alpha demo SDK.

This demo shows:

- executable-local top-level handlers such as `--verbose` and `--output`
- imported SDK inline parsing through `AlphaSdk.GetInlineParser()`
- aliases for both top-level and inline options
- bare-root help for the imported `--alpha` parser
