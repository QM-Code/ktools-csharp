# Omega Demo

Full-featured CLI integration showcase for KcliSDK plus the demo SDKs.

This demo exercises the full demo API:

- multiple imported inline parsers
- inline root override via `--newgamma-*`
- local build parsing through `--build-*`
- aliases plus positional handling in the same parser
