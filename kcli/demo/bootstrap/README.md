# Bootstrap Demo

Exists for CI and as the smallest compile/link usage reference for KcliSDK.

This demo shows the minimal executable-side setup:

- create a `Kcli.Parser`
- register a top-level handler and alias
- call `ParseOrExit(...)`
