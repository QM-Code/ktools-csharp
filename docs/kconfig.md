# `kconfig` In `ktools-csharp`

`ktools-csharp/` does not currently include a checked-in `kconfig`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `kconfig` remains a planned layer rather than a shipped C# component today
- the website keeps this page because the language/tool grid is part of the
  overall ktools surface area
