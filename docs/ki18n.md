# `ki18n` In `ktools-csharp`

`ktools-csharp/` does not currently include a checked-in `ki18n`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `ki18n` remains planned rather than implemented in the current C# workspace
- future work should add canonical markdown here rather than website-only copy
